config.suffixes.add(".py")

config.excludes.add("golden")
config.excludes.add("op_by_op")
config.excludes.add("chisel")
config.excludes.add("runtime")

if not config.enable_bindings_python:
    config.unsupported = True
